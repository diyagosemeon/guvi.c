#include<stdio.h>
#include<conio.h>
int main()
{
int a=size of (arr[0]);
int arr={2,3,4,5};
int sum=0;
for(int i=0;i<=a;i++)
{
sum += arr[1];
}
printf("Sum of given array is %d", sum(arr, n)); 
return 0;
}

