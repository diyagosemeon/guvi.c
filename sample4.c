#include<stdio.h>
#include<conio.h>
int main()
{
int a,b.c;
printf("enter the element");
scanf("%d %d %d",&a,&b,&c);
if(a>=b && a>+c)
{
printf("%d is largest no",a);
}
elseif(b>=a && b>=a)
{
printf("%d is largest no",b);
}
else
{
printf("%d is largest no",c);
}
return 0;
}

